# 변경 이력

근태관리 앱의 사용자 기능, 권한, 보안, 데이터베이스 변경을 기록한다. 커밋의 세부 구현보다 실제 사용자와 운영자에게 달라진 내용을 중심으로 작성한다.

## 2026-08-10

### 추가

- 기관관리자와 최고관리자의 로그인 성공 이력을 기록하는 기능을 추가했다.
- 로그인 시각, 서버가 확인한 접속 IP, 기기와 브라우저 정보를 기록한다.
- 최고관리자 화면에 모든 기관관리자와 최고관리자의 로그인 이력 메뉴를 추가했다.
- 기관관리자 화면에 본인의 로그인 이력만 확인하는 메뉴를 추가했다.
- 관리자 로그인 이력 전용 데이터베이스 표와 조회 API를 추가했다.

### 보안과 개인정보

- 로그인 이력에는 GPS와 실제 위치를 수집하지 않는다.
- 기관관리자는 본인 기록만 조회할 수 있고 접속 IP 일부를 가려서 표시한다.
- 최고관리자만 전체 관리자 기록과 전체 IP를 확인할 수 있다.
- 로그인 이력은 1년이 지난 기록부터 정리한다.
- 이력 저장에 일시적인 오류가 생겨도 정상 로그인을 막지 않는다.

### 확인

- 자동 점검 128개를 통과했다.
- Vinext 운영 빌드를 통과했다.

### 수정

- GitHub 기관 동기화 과정에서 발견된 로그인 이력 저장 코드의 타입 오류를 수정했다.
- Supabase 요청 객체에 오류 처리를 직접 연결하던 코드를 일반 예외 처리 방식으로 바꿔 기관 저장소의 타입검사를 통과하도록 보완했다.
- 다기관 환경에서 시간외근무 등 통합 요청을 저장할 때 오류 코드 `21000`으로 신청이 실패하던 문제를 수정했다.
- 승인 예외 동기화 기능이 예전 단일기관 설정 대신 신청이 속한 기관의 근무시간 설정만 조회하도록 변경했다.
- 기존 운영 데이터베이스에 같은 수정을 다시 적용할 수 있는 보완 SQL을 추가했다.
- 평일 시간외근무 신청에서 기관 정규 근무시간을 제외하고, 근무시간 이전과 이후만 합산하도록 계산을 수정했다.
- 주말과 기관 휴일의 시간외근무 신청은 입력한 실제 근무시간 전체를 계산하도록 구분했다.
- 기관관리자가 자기 기관 직원의 근태기록에 휴가 또는 대체휴무를 반영하지 못하던 `P0001` 권한 오류를 수정했다.
- 기관관리자가 승인되어 출근부에 반영된 휴가를 재검토한 뒤 수정하거나 반려할 수 있도록 보완했다.
- 휴가 승인을 되돌리거나 다시 승인하면 출근부의 휴가 표시도 신청 상태에 맞춰 자동으로 갱신되도록 했다.
- 시간외근무 화면과 내보내기 자료에서 불필요한 `일반` 표현을 제거하고, 값이 없는 시간외근무는 `0시간`으로 함께 표시하지 않도록 했다.
- 결혼·가족 장례 등 특별휴가와 장기재직휴가 등 기타 휴가를 시간 대신 일수로 표시하며, 여러 날의 휴가는 출근부에서 근무일마다 `1일`로 표시하도록 수정했다.
- 특별휴가는 경조사 휴가, 기타 휴가는 장기재직휴가 등 기관 자체휴가로 구분해 입력 예시를 바로잡았다.
- 같은 날 연차, 대체휴무 등 여러 휴가가 승인된 경우 출근부에 모든 항목을 함께 표시하도록 수정했다.
- 출근부의 승인 시간외근무에 신청 사유를 함께 표시하도록 보완했다.
- 출퇴근 기록이 없는 연차, 대체휴무, 출장 등 승인 예외 일정도 해당 날짜의 월별 근태기록에 자동으로 표시하도록 보완했다.
- 과거 승인 당시 예외 일정이 생성되지 않은 특별휴가와 기타 휴가도 승인 신청을 기준으로 날짜별 근태기록에 표시하도록 보완했다.
- 날짜별로 표시한 출장 예외를 월별 요약에서 다시 더해 1일 출장이 2일로 집계되던 중복 계산을 수정했다.
- 총 근무일과 출장일은 같은 직원의 같은 날짜를 한 번만 세도록 변경했다.
- 승인된 휴가·대체휴무와 출장·예외 일정 목록을 사용 시작일이 빠른 순으로 정렬하고, 같은 날짜는 직원명 순으로 표시하도록 정리했다.
- 근태기록 표에서 `최근 날짜 먼저`와 `과거 날짜 먼저`를 관리자가 직접 선택할 수 있는 날짜 순서 필터를 추가했다.
